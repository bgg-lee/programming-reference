#include <iostream>
#include <vector>
#include <algorithm>
#include <set>
#include <unordered_set>
#include <unordered_map>
using namespace std;
#include <string>
#include <queue>
int main(){
    vector<int> vec = {5,3,1,6};
    vector<int> temp;

    for(auto x : temp){
        cout<<x<<" ";
    }
    // priority_queue<int> q;
    // q.push(3);
    // q.push(5);
    // find(q.begin(),q.end(),5);
    // q.

    // string s = "cabczf";
    // cout << s[2] << endl;
    // s[2] = 'a';
    // cout << s << endl;
    // cout << s[2]+0 << endl;
    // unordered_map<char,int> m = {{'a',1},{'b',2}};
    // // cout << *m.find('a');
    // if(m.find('c')!=m.end()) cout << "find it" << endl;
    // else cout<<"end"<<endl;
    // return -1;

    // vector<int> vec = {5,1,2,5,65,634};
    // unordered_set<int> s(vec.begin(),vec.end());
    // // vector<int> vec2 = vec; 

    // // sort(vec.begin(),vec.end());
    // // for(int a : vec){
    // //     cout<<a<<" ";
    // // }
    // // cout<<endl;
    // // for(int a : vec2){
    // //     cout<<a<<" ";
    // // }
    // auto it = s.find(65);
    // cout << s.count(5) << endl;
    // cout << s.count(115) << endl;
    // // if(find(s.begin(),s.end(),65)) cout << 65 << endl;
    // if(s.find(615)!=s.end()) cout << 65 << endl;
    // cout << (s.begin()+it);
}